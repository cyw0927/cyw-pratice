# SOLUTION GUIDE

> 담당 범위: **문제 2 — 컬러 이미지의 임계값을 계산하고 흑백 이미지 만들기**
>
> `problem02_learning.ipynb`에는 75개의 상세 학습 질문과 실행 가능한 코드 셀이 있고, `AI_LOG.md`에는 단계별 질문 과정, `TEST_RESULTS.md`에는 테스트 기록, `problem02.py`에는 최종 실행 코드가 있다.

---

## 1. 문제를 내 말로 다시 설명하기

이 문제는 실제 JPG나 PNG 파일을 편집하는 문제가 아니라, RGB 숫자로 주어진 이미지 데이터를 0과 1로 바꾸는 문제다. 이미지의 한 픽셀은 `R,G,B` 세 숫자로 표현된다. 먼저 각 픽셀을 `(R + G + B) // 3`으로 계산해서 하나의 밝기 값으로 만든다. 그다음 모든 픽셀의 밝기를 더하고 전체 픽셀 수 `H * W`로 나눠 평균 밝기인 `threshold`를 구한다. 각 픽셀의 밝기가 threshold보다 크거나 같으면 `1`, 작으면 `0`으로 바꾼다. 첫 줄에는 threshold를 출력하고, 그 아래에는 원래 이미지와 같은 행 구조로 0/1 결과를 출력한다. 따라서 핵심은 **입력 파싱, 밝기 계산, 평균 계산, 조건 비교, 행 구조 유지**다.

---

## 2. 사용한 핵심 개념

### RGB
픽셀 하나의 색을 Red, Green, Blue 세 값으로 나타낸다. 이 문제에서는 각 값을 평균내 밝기로 바꾼다.

### 문자열과 `split()`
`input()`으로 들어온 값은 문자열이다. 한 줄에서는 공백으로 픽셀을 나누고, 한 픽셀에서는 쉼표로 R,G,B를 나눈다.

### `int()`와 `map()`
`'255'` 같은 문자열은 계산 전에 정수 255로 변환해야 한다. `map(int, ...)`을 쓰면 여러 문자열을 한 번에 정수로 바꿀 수 있다.

### 리스트와 2차원 리스트
한 행의 밝기를 리스트로 만들고, 여러 행을 다시 리스트에 넣으면 이미지 구조를 유지할 수 있다.

### `for`문과 중첩 반복문
이미지는 행과 열로 구성되므로 바깥 반복문은 행, 안쪽 반복문은 픽셀을 담당한다.

### `append()`
계산한 밝기나 완성된 행을 리스트 끝에 추가할 때 사용한다.

### 정수 나눗셈 `//`
평균을 계산한 뒤 소수점 아래를 버린 정수 값을 만든다. 이 문제의 brightness와 threshold는 모두 정수다.

### `if`문과 `>=`
`brightness >= threshold`이면 1, 아니면 0이다. 같은 값도 1이어야 하므로 `>`가 아니라 `>=`가 필요하다.

### 함수
현재 코드는 역할을 `calc_brightness`, `parse_pixel`, `build_brightness_map`, `calc_threshold`, `to_black_white`, `solve`, `main`으로 나눴다. 이렇게 하면 각 기능을 따로 실행하고 테스트하기 쉽다.

---

## 3. 문제 해결을 작은 단계로 나누기 — 75단계 관점

`problem02_learning.ipynb`에서는 문제를 75개의 질문으로 나눴다. 큰 흐름은 다음과 같다.

1. 문제의 최종 결과가 실제 이미지 파일이 아니라 threshold와 0/1 데이터라는 점을 이해한다.
2. RGB와 픽셀의 의미를 이해한다.
3. RGB 값의 범위 0~255를 이해한다.
4. 입력값은 문자열이라는 점을 확인한다.
5. 공백과 쉼표의 역할을 구분한다.
6. `split()`과 `map(int, ...)`으로 RGB를 정수로 만든다.
7. `(R+G+B)//3`으로 밝기를 계산한다.
8. `/`와 `//`의 차이를 확인한다.
9. H와 W의 의미를 확인한다.
10. 전체 픽셀 수가 `H*W`라는 점을 확인한다.
11. 반복문으로 모든 행을 읽는다.
12. 중첩 반복문으로 모든 픽셀을 처리한다.
13. 한 행의 밝기를 `brightness_row`에 저장한다.
14. 전체 이미지를 `brightness_map`에 저장한다.
15. 모든 밝기를 더해 total을 구한다.
16. `total // (H*W)`로 threshold를 계산한다.
17. threshold를 첫 줄에 출력한다.
18. 각 밝기를 threshold와 비교한다.
19. 이상이면 1, 미만이면 0으로 바꾼다.
20. 한 행을 문자열로 만들어 출력 형식을 맞춘다.
21. `>=`의 경계값을 확인한다.
22. 예제 입력을 손으로 추적한다.
23. 전체 검정, 전체 흰색, 1×1 같은 극단값을 테스트한다.
24. 서로 다른 RGB가 같은 밝기를 가질 수 있음을 확인한다.
25. 시간 복잡도를 픽셀 수 기준으로 생각한다.
26. 함수별 책임을 분리한다.
27. 입력과 계산 로직을 분리해 테스트 가능하게 만든다.
28. `solve()`를 이용해 Notebook과 테스트 코드에서도 같은 로직을 재사용한다.
29. 파일 경로, 가상환경, Jupyter 커널 등 실행 환경 오류를 구분한다.
30. traceback의 마지막 줄부터 읽어 오류 원인을 찾는다.
31. 정답 코드만 외우지 않고 각 문법이 왜 필요한지 설명한다.
32. AI 답변은 예제와 직접 실행으로 검증한다.
33. 다른 구현 방법과 현재 방법의 장단점을 비교한다.
34. RGB 정보가 밝기 한 값으로 줄어들면서 색 정보가 사라진다는 점을 이해한다.
35. 전체 풀이를 코드 없이도 설명할 수 있는지 확인한다.

위 단계들은 Notebook에서 더 세분화되어 Q01~Q75로 정리되어 있다.

---

## 4. 수도코드

```text
H, W를 입력받는다
RGB 행들을 저장한다

각 행의 각 픽셀에 대해
    쉼표 기준으로 R, G, B를 나눈다
    정수로 바꾼다
    brightness = (R + G + B) // 3
    brightness_map에 행 구조대로 저장한다

모든 brightness를 더한다
threshold = 전체 합 // (H * W)

각 brightness에 대해
    threshold 이상이면 1
    아니면 0

threshold를 먼저 출력한다
그다음 결과 행들을 순서대로 출력한다
```

---

## 5. 최종 코드 구조

현재 `problem02.py`는 다음 함수들로 구성되어 있다.

```python
def calc_brightness(r, g, b):
    return (r + g + b) // 3


def parse_pixel(pixel_text):
    r, g, b = map(int, pixel_text.split(","))
    return r, g, b


def build_brightness_map(rgb_rows):
    brightness_map = []
    for pixels in rgb_rows:
        brightness_row = []
        for pixel in pixels:
            r, g, b = parse_pixel(pixel)
            brightness_row.append(calc_brightness(r, g, b))
        brightness_map.append(brightness_row)
    return brightness_map


def calc_threshold(brightness_map, height, width):
    total = 0
    for row in brightness_map:
        for brightness in row:
            total += brightness
    return total // (height * width)


def to_black_white(brightness_map, threshold):
    result = []
    for row in brightness_map:
        result_row = ""
        for brightness in row:
            if brightness >= threshold:
                result_row += "1"
            else:
                result_row += "0"
        result.append(result_row)
    return result


def solve(height, width, rgb_rows):
    brightness_map = build_brightness_map(rgb_rows)
    threshold = calc_threshold(brightness_map, height, width)
    result = to_black_white(brightness_map, threshold)
    return threshold, result
```

`main()`은 실제 `input()`과 `print()`만 담당하고, 계산은 `solve()` 아래 함수들이 담당한다.

---

## 6. 주요 코드 한 줄씩 설명하기

### `r, g, b = map(int, pixel_text.split(','))`
`255,0,0`을 먼저 `['255','0','0']`으로 나눈 뒤 세 값을 정수로 바꿔 각각 r, g, b에 저장한다.

### `return (r + g + b) // 3`
세 색 성분의 정수 평균을 밝기로 반환한다.

### `brightness_row.append(...)`
현재 행의 픽셀 밝기를 순서대로 저장한다.

### `brightness_map.append(brightness_row)`
완성된 한 행을 전체 이미지 밝기 맵에 추가한다.

### `total += brightness`
각 픽셀의 밝기를 하나씩 누적해 전체 합을 만든다.

### `total // (height * width)`
전체 합을 전체 픽셀 수로 나눠 평균 threshold를 만든다.

### `if brightness >= threshold:`
현재 픽셀이 평균 이상인지 확인한다. 같은 값도 1이 되어야 하므로 `>=`다.

### `result_row += '1'` / `result_row += '0'`
한 행의 결과를 문제에서 요구한 `110` 같은 문자열로 만든다.

### `return threshold, result`
계산 결과를 출력과 분리해서 반환한다. 덕분에 Notebook과 테스트 코드에서 직접 비교할 수 있다.

---

## 7. 예제 데이터 직접 추적

입력:

```text
3 3
255,255,255 200,200,200 0,0,0
120,120,120 60,60,60 30,30,30
255,0,0 0,255,0 0,0,255
```

| 위치 | RGB | 밝기 |
|---|---|---:|
| 1행 1열 | 255,255,255 | 255 |
| 1행 2열 | 200,200,200 | 200 |
| 1행 3열 | 0,0,0 | 0 |
| 2행 1열 | 120,120,120 | 120 |
| 2행 2열 | 60,60,60 | 60 |
| 2행 3열 | 30,30,30 | 30 |
| 3행 1열 | 255,0,0 | 85 |
| 3행 2열 | 0,255,0 | 85 |
| 3행 3열 | 0,0,255 | 85 |

전체 합은 920, 픽셀 수는 9다.

```text
threshold = 920 // 9 = 102
```

| 밝기 행 | 비교 | 결과 |
|---|---|---|
| 255, 200, 0 | 1, 1, 0 | 110 |
| 120, 60, 30 | 1, 0, 0 | 100 |
| 85, 85, 85 | 0, 0, 0 | 000 |

최종 출력:

```text
102
110
100
000
```

---

## 8. 오류와 시행착오

### 시행착오 1. 파일 경로 오류
처음에는 터미널 현재 위치와 실제 파일 위치가 달라 `can't open file` 오류가 발생했다. 파일 경로와 터미널 위치를 비교하면서 Python 실행 오류와 코드 내부 오류가 서로 다르다는 점을 배웠다.

### 시행착오 2. `>`와 `>=` 차이
처음에는 단순히 큰 값만 1이라고 생각하기 쉽지만 문제는 **이상**이라고 되어 있다. threshold와 같은 값도 1이어야 하므로 `>=`가 필요하다.

### 시행착오 3. 검은색이 전부 1이 되는 결과
전체 검정 이미지는 brightness=0, threshold=0이다. `0 >= 0`이 참이므로 결과가 모두 1이 된다. 직관보다 문제 조건이 우선이라는 점을 확인했다.

### 시행착오 4. Notebook의 코드가 실제 셀이 아니었던 문제
설명 속 코드블록만 있으면 VS Code에서 바로 실행하기 어렵다. 이후 `.ipynb`를 실제 Markdown 셀과 Code 셀로 분리해 각 코드가 직접 실행되도록 바꿨다.

---

## 9. 테스트 케이스

`TEST_RESULTS.md`에 총 8개 테스트를 상세 기록했다.

- 과제 공식 예제
- 전체 검정
- 전체 흰색
- 1×1 이미지
- 밝고 어두운 값 혼합
- 서로 다른 RGB지만 같은 밝기
- 한 행짜리 이미지
- threshold와 정확히 같은 경계값

각 테스트에는 입력, 예상 결과, 실제 결과, PASS/FAIL과 확인한 점을 기록했다.

---

## 10. 시간 복잡도 / 처리량

픽셀 수는 `H * W`개다. 입력과 밝기 계산, 전체 합 계산, 0/1 변환에서 모든 픽셀을 일정 횟수씩 확인한다. 픽셀을 몇 번 순회하더라도 반복 횟수는 상수배이므로 시간 복잡도는 `O(HW)`로 볼 수 있다.

현재 구현은 brightness_map 전체를 저장하므로 추가 공간도 `O(HW)`가 필요하다. 입력과 동시에 합을 계산하는 식으로 일부 단계를 합칠 수 있지만 threshold를 구한 뒤 다시 각 픽셀을 비교해야 하므로 어떤 형태로든 픽셀 밝기 정보를 보관해야 한다.

---

## 11. 다른 해결 방법

### 1차원 리스트 사용
모든 brightness를 한 리스트에 넣고 인덱스로 행을 다시 나눌 수 있다. 합 계산은 단순하지만 이미지의 행 구조가 덜 직관적이다.

### `sum()` 활용
중첩 반복문 대신 각 행의 `sum(row)`를 더해 전체 합을 계산할 수 있다. 코드는 짧아지지만 학습 단계에서는 중첩 반복문이 더 이해하기 쉽다.

### `''.join()` 활용
`result_row += '1'` 방식 대신 문자 리스트를 만든 뒤 `''.join()`으로 합칠 수 있다. 큰 데이터에서는 문자열을 반복 연결하는 것보다 효율적일 수 있다.

### 실제 영상 처리의 가중 밝기 공식
실제 영상 처리에서는 R,G,B를 같은 비중으로 보지 않는 공식도 있다. 하지만 이 과제에서는 반드시 문제에서 지정한 `(R+G+B)//3`을 사용해야 한다.

---

## 12. AI를 사용하면서 배운 점

가장 도움이 된 질문은 다음과 같은 종류였다.

1. 문제 전체 흐름을 코드 없이 설명해 달라고 한 질문
2. `split()`과 `map()`을 실제 입력 예제로 설명해 달라고 한 질문
3. `>`와 `>=`의 차이를 경계값으로 설명해 달라고 한 질문
4. 전체 검정 이미지가 왜 전부 1이 되는지 물은 질문
5. 예제의 모든 변수값을 단계별로 추적해 달라고 한 질문

처음에는 “코드가 왜 안 되냐”처럼 너무 넓게 질문하면 원인을 찾기 어려웠다. 이후 “현재 터미널 경로는 어디고 파일은 어디 있나?”, “brightness가 threshold와 같을 때 결과가 무엇인가?”처럼 범위를 좁히니 이해가 빨라졌다.

AI 답변은 그대로 믿지 않고 직접 계산과 실행으로 확인했다. 특히 threshold 102, 전체 검정 결과, 1×1 결과, 같은 밝기를 가진 서로 다른 RGB를 직접 테스트하면서 설명과 실제 코드가 일치하는지 검증했다.

---

## 13. 마지막 3문장 정리

처음에는 RGB, brightness, threshold가 각각 따로 보였고 실제 이미지를 편집하는 문제인지도 헷갈렸다. 지금은 문자열 입력을 RGB 정수로 바꾸고, 밝기와 평균을 계산한 뒤 `>=` 조건으로 0/1 결과를 만드는 전체 흐름을 설명할 수 있다. 다시 풀면 먼저 작은 예제를 손으로 추적하고, 기능을 함수별로 나눈 뒤 각 함수를 개별 테스트하면서 구현할 것이다.

---

## 14. 최신 Notebook 구조와 문서 일치 확인

최신 `problem02_learning.ipynb`는 표지 1개와 Q01~Q75의 Markdown/Code 셀 75쌍으로 총 151개 셀이다. 초반에는 RGB 한 픽셀의 작은 계산을 확인하고, 이후 한 행과 전체 이미지로 범위를 넓힌다. 핵심 지점인 Q14, Q18, Q25, Q35, Q38, Q43, Q49, Q65, Q72, Q75에서는 이전 개념을 합친 더 긴 코드로 중간 변수를 추적한다.

각 질문의 설명은 다음 공통 구조를 가진다.

1. 학습 단계와 이번 질문의 목표
2. 이전 단계와의 연결
3. 상세 설명과 실행 전 예상
4. 확인할 중간 변수
5. 헷갈린 점과 확인 방법
6. 실행 결과를 읽는 기준
7. 학습 정리와 다음 질문 연결

최종 검증에서는 코드 셀 75개가 모두 순서대로 실행되었고 오류 출력은 없었다. 마지막 전체 재구성 셀은 밝기 맵 `[[255, 200, 0], [120, 60, 30], [85, 85, 85]]`, threshold `102`, 결과 `['110', '100', '000']`을 출력하고 `assert`로 다시 확인한다.

`AI_LOG.md`도 Notebook의 Q번호와 주제를 1:1로 맞췄다. AI 로그는 실제 대화 원문을 꾸민 문서가 아니라 각 학습 질문, 이해한 핵심, 실행 근거와 다음 단계를 연결한 재현 가능한 학습·검증 기록이다.

