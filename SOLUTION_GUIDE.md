# SOLUTION GUIDE

> 현재 담당 범위는 **문제 2: 컬러 이미지의 임계값을 계산하고 흑백 이미지 만들기**이다. 문제 1은 다른 담당 범위이므로 이 문서에서는 문제 2를 중심으로 정리한다.

## 1. 문제를 내 말로 다시 설명하기

### 문제 2
컬러 이미지는 각 픽셀이 R, G, B 세 숫자로 표현된다. 이 세 숫자를 그대로 비교하기는 복잡하므로 먼저 `(R + G + B) // 3`으로 하나의 밝기 값으로 바꾼다. 모든 픽셀의 밝기를 구한 뒤 그 밝기들의 평균을 계산한다. 이 평균값을 임계값(threshold)으로 사용한다. 각 픽셀의 밝기가 threshold보다 크거나 같으면 `1`로 바꾸고, 작으면 `0`으로 바꾼다. 이렇게 하면 컬러 이미지가 0과 1로만 이루어진 흑백 이미지가 된다. 첫 줄에는 계산된 threshold를 출력하고, 그 아래에는 원래 이미지와 같은 크기의 0/1 결과를 출력한다.

---

## 2. 사용한 핵심 개념

### RGB
RGB는 Red, Green, Blue의 줄임말이다. 하나의 픽셀은 세 색의 세기를 숫자로 가지고 있으며, 이 문제에서는 각 값이 0부터 255까지 사용된다.

### 리스트
여러 값을 순서대로 저장하는 자료구조이다. 여러 픽셀의 밝기나 결과를 저장할 때 사용한다.

### 2차원 리스트
리스트 안에 리스트가 있는 구조이다. 이미지가 행과 열로 이루어져 있으므로 이미지 모양을 그대로 저장하기 좋다.

### `for`문
같은 작업을 여러 번 반복할 때 사용한다. 이미지의 모든 행과 모든 픽셀을 하나씩 확인하기 위해 사용한다.

### 중첩 반복문
`for`문 안에 또 다른 `for`문이 있는 형태이다. 바깥 반복문은 행을 이동하고, 안쪽 반복문은 그 행 안의 픽셀을 이동한다.

### `if`문
조건에 따라 다른 작업을 실행한다. 밝기가 threshold 이상인지 미만인지 확인할 때 사용한다.

### 함수
특정 기능을 따로 묶어 놓은 코드이다. 이 문제에서는 밝기 계산, threshold 계산, 흑백 변환을 각각 함수로 나누었다.

### 문자열과 `split()`
입력되는 `255,0,0`은 처음에는 문자열이다. `split(',')`을 사용하면 쉼표를 기준으로 `255`, `0`, `0` 세 부분으로 나눌 수 있다.

### `int()`와 `map()`
문자열 상태의 숫자는 계산할 수 없으므로 `int()`로 정수로 바꾼다. `map(int, ...)`을 사용하면 여러 문자열을 한 번에 정수로 변환할 수 있다.

### `append()`
리스트 끝에 새 값을 추가한다. 계산한 밝기나 한 줄의 결과를 저장할 때 사용한다.

### 정수 나눗셈 `//`
나눗셈 결과에서 소수점 아래를 버리고 정수 몫만 얻는다. 이 문제에서는 밝기와 threshold를 정수로 계산하기 위해 사용한다.

### `>=`
왼쪽 값이 오른쪽 값보다 크거나 같은지 확인한다. threshold와 같은 밝기도 `1`이 되어야 하므로 `>`가 아니라 `>=`를 사용한다.

---

## 3. 문제 해결을 작은 단계로 나누기

### 문제 2
1. 입력 첫 줄에서 이미지 높이 `H`와 너비 `W`를 읽는다.
2. `H`가 몇 줄의 RGB 데이터를 입력받아야 하는지 의미한다는 것을 확인한다.
3. `W`가 한 줄에 몇 개의 픽셀이 있는지 의미한다는 것을 확인한다.
4. 밝기를 저장할 빈 2차원 리스트를 만든다.
5. 이미지 높이만큼 반복한다.
6. 한 줄의 RGB 데이터를 문자열로 입력받는다.
7. 공백을 기준으로 한 줄의 픽셀들을 나눈다.
8. 각 픽셀 문자열을 하나씩 확인한다.
9. `255,0,0` 같은 문자열을 쉼표 기준으로 나눈다.
10. 세 조각을 정수로 변환한다.
11. R, G, B 변수에 각각 저장한다.
12. `(R + G + B) // 3`으로 밝기를 계산한다.
13. 계산한 밝기를 현재 행 리스트에 추가한다.
14. 한 행의 모든 픽셀 처리가 끝나면 밝기 행을 전체 밝기 맵에 추가한다.
15. 모든 행 입력이 끝나면 전체 픽셀의 밝기가 2차원 리스트에 저장된다.
16. 전체 밝기의 합을 구하기 위해 `total = 0`으로 시작한다.
17. 모든 행을 반복한다.
18. 각 행의 모든 밝기를 반복한다.
19. 각 밝기를 `total`에 계속 더한다.
20. 전체 픽셀 수를 `H * W`로 계산한다.
21. `total // (H * W)`로 threshold를 계산한다.
22. threshold를 출력한다.
23. 흑백 결과를 저장할 빈 리스트를 만든다.
24. 밝기 맵의 각 행을 다시 반복한다.
25. 한 행의 결과를 저장할 빈 문자열을 만든다.
26. 행 안의 각 밝기를 threshold와 비교한다.
27. 밝기가 threshold 이상이면 문자열에 `1`을 붙인다.
28. 밝기가 threshold보다 작으면 문자열에 `0`을 붙인다.
29. 한 행 처리가 끝나면 결과 문자열을 결과 리스트에 추가한다.
30. 모든 결과 행을 순서대로 출력한다.

---

## 4. 수도코드

### 문제 2

```text
H, W를 입력받는다
밝기맵을 빈 리스트로 만든다

H번 반복한다
    한 줄의 픽셀들을 입력받는다
    현재 행의 밝기 리스트를 만든다

    각 픽셀에 대해 반복한다
        쉼표 기준으로 R, G, B를 나눈다
        숫자로 변환한다
        밝기 = (R + G + B) // 3
        현재 행에 밝기를 추가한다

    밝기맵에 현재 행을 추가한다

전체 밝기 합을 구한다
전체 픽셀 수 = H * W
threshold = 전체 밝기 합 // 전체 픽셀 수
threshold를 출력한다

각 밝기 행에 대해 반복한다
    결과 문자열을 빈 문자열로 만든다

    각 밝기에 대해 반복한다
        밝기 >= threshold 이면 1을 붙인다
        아니면 0을 붙인다

    완성된 한 줄을 저장한다

모든 결과 행을 출력한다
```

---

## 5. 최종 코드

현재 `problem02.py`에 작성한 최종 코드는 다음과 같다.

```python
def calc_brightness(r, g, b):
    return (r + g + b) // 3


def calc_threshold(brightness_map, height, width):
    total = 0

    for row in brightness_map:
        for brightness in row:
            total += brightness

    pixel_count = height * width
    return total // pixel_count


def to_black_white(brightness_map, threshold):
    result = []

    for row in brightness_map:
        result_row = ""

        for brightness in row:
            if brightness >= threshold:
                result_row += "1"
            else:
                result_row += "0"

        result.append(result_row)

    return result


def main():
    height, width = map(int, input().split())
    brightness_map = []

    for _ in range(height):
        pixels = input().split()
        brightness_row = []

        for pixel in pixels:
            r, g, b = map(int, pixel.split(","))
            brightness = calc_brightness(r, g, b)
            brightness_row.append(brightness)

        brightness_map.append(brightness_row)

    threshold = calc_threshold(brightness_map, height, width)
    print(threshold)

    black_white_image = to_black_white(brightness_map, threshold)

    for row in black_white_image:
        print(row)


if __name__ == "__main__":
    main()
```

---

## 6. 코드 설명

### `calc_brightness(r, g, b)`
한 픽셀의 R, G, B 값을 받아 평균을 구한다. 예를 들어 빨간색 `(255, 0, 0)`은 `(255 + 0 + 0) // 3 = 85`가 된다.

### `calc_threshold(brightness_map, height, width)`
이미지의 모든 밝기를 더한 뒤 전체 픽셀 수로 나눈다. `height * width`가 전체 픽셀 수이다. 이 평균값을 이미지 전체의 밝고 어두움을 나누는 기준으로 사용한다.

### `to_black_white(brightness_map, threshold)`
모든 밝기를 threshold와 비교한다. threshold 이상이면 `1`, 미만이면 `0`을 붙여 한 행의 결과 문자열을 만든다.

### `height, width = map(int, input().split())`
예를 들어 `3 3`을 입력하면 문자열 `3`, `3`을 나눈 후 정수로 바꾸어 `height = 3`, `width = 3`으로 저장한다.

### `pixels = input().split()`
한 줄에 여러 픽셀이 공백으로 구분되어 있으므로 각 픽셀 문자열을 따로 분리한다.

### `r, g, b = map(int, pixel.split(','))`
`255,0,0`을 쉼표 기준으로 세 조각으로 나누고 각각 정수로 바꾼다.

### `brightness_row.append(brightness)`
계산한 밝기를 현재 행에 추가한다.

### `brightness_map.append(brightness_row)`
한 행의 밝기 계산이 끝나면 이미지 전체 밝기 맵에 그 행을 추가한다.

### `if brightness >= threshold`
밝기가 기준값과 같아도 `1`이어야 하므로 `>=`를 사용한다.

### `result_row += '1'` / `result_row += '0'`
각 픽셀의 흑백 결과를 한 줄 문자열에 이어 붙인다.

---

## 7. 예제 데이터 직접 추적

입력:

```text
3 3
255,255,255 200,200,200 0,0,0
120,120,120 60,60,60 30,30,30
255,0,0 0,255,0 0,0,255
```

각 픽셀의 밝기:

| 위치 | RGB | 밝기 계산 | 밝기 |
|---|---|---|---:|
| 1행 1열 | 255,255,255 | 765 // 3 | 255 |
| 1행 2열 | 200,200,200 | 600 // 3 | 200 |
| 1행 3열 | 0,0,0 | 0 // 3 | 0 |
| 2행 1열 | 120,120,120 | 360 // 3 | 120 |
| 2행 2열 | 60,60,60 | 180 // 3 | 60 |
| 2행 3열 | 30,30,30 | 90 // 3 | 30 |
| 3행 1열 | 255,0,0 | 255 // 3 | 85 |
| 3행 2열 | 0,255,0 | 255 // 3 | 85 |
| 3행 3열 | 0,0,255 | 255 // 3 | 85 |

전체 밝기 합은 `920`이고 전체 픽셀 수는 `3 × 3 = 9`이다.

```text
threshold = 920 // 9 = 102
```

threshold와 비교하면 다음과 같다.

| 밝기 행 | 비교 결과 | 출력 |
|---|---|---|
| 255, 200, 0 | 255≥102, 200≥102, 0<102 | 110 |
| 120, 60, 30 | 120≥102, 60<102, 30<102 | 100 |
| 85, 85, 85 | 모두 102 미만 | 000 |

최종 출력:

```text
102
110
100
000
```

직접 `problem02.py`를 실행해 위 입력을 넣었고 같은 출력이 나오는 것을 확인했다.

---

## 8. 오류와 시행착오

### 오류 1. Python이 `problem02.py`를 찾지 못함
- 발생한 오류: `can't open file ... problem02.py: [Errno 2] No such file or directory`
- 원인: 터미널의 현재 위치는 `cyw-pratice-main`이었지만 `problem02.py`는 그 아래 `image_algorithm_quiz` 폴더에 있었다.
- 원인을 찾은 방법: VS Code 탐색기에서 실제 파일 위치와 터미널 현재 경로를 비교했다.
- 해결 방법: `cd image_algorithm_quiz`로 이동한 뒤 `python problem02.py`를 실행했다.
- 배운 점: Python 파일을 실행할 때는 현재 터미널 위치와 실제 파일 경로를 확인해야 한다.

### 오류 2. `>`와 `>=`를 혼동할 수 있음
- 확인한 문제: threshold와 같은 밝기를 처리할 때 `>`를 쓰면 과제 규칙과 다른 결과가 나온다.
- 원인: `>`는 같은 값을 포함하지 않지만 `>=`는 같은 값도 포함한다.
- 확인 방법: brightness와 threshold를 둘 다 `102`로 두고 조건을 비교해 보았다.
- 올바른 처리: `brightness >= threshold`를 사용한다.
- 배운 점: 문제에 적힌 '이상'과 '초과'를 정확히 구분해야 한다.

### 오류 3. RGB 문자열을 숫자로 바꾸지 않으면 계산할 수 없음
- 확인한 문제: 입력값 `255,0,0`은 처음에는 문자열이므로 그대로 더하거나 평균을 계산하면 원하는 숫자 계산이 되지 않는다.
- 원인: 입력 함수 `input()`의 결과는 문자열이다.
- 확인 방법: `split(',')`의 결과가 문자열 세 개라는 것을 확인했다.
- 해결 방법: `map(int, pixel.split(','))`로 R, G, B를 정수로 바꾸었다.
- 배운 점: 입력받은 데이터의 자료형을 확인하는 것이 중요하다.

---

## 9. 테스트 케이스

### Test 1. 과제 예제

입력:
```text
3 3
255,255,255 200,200,200 0,0,0
120,120,120 60,60,60 30,30,30
255,0,0 0,255,0 0,0,255
```

예상 결과:
```text
102
110
100
000
```

직접 실행 결과: 예상 결과와 일치함 — **PASS**

### Test 2. 한 픽셀 흰색 이미지

입력:
```text
1 1
255,255,255
```

예상 결과:
```text
255
1
```

설명: 픽셀이 하나뿐이므로 밝기 255가 그대로 threshold가 되고, 밝기와 threshold가 같으므로 1이 된다.

### Test 3. 한 픽셀 검은색 이미지

입력:
```text
1 1
0,0,0
```

예상 결과:
```text
0
1
```

설명: threshold도 0이고 밝기도 0이다. 조건이 `>=`이므로 1이 된다.

### Test 4. 검정과 흰색 두 픽셀

입력:
```text
1 2
0,0,0 255,255,255
```

예상 결과:
```text
127
01
```

설명: 평균은 `(0 + 255) // 2 = 127`이다. 검정은 127보다 작아서 0, 흰색은 127 이상이라 1이다.

### Test 5. 모든 픽셀이 같은 색

입력:
```text
2 2
100,100,100 100,100,100
100,100,100 100,100,100
```

예상 결과:
```text
100
11
11
```

설명: 모든 픽셀의 밝기가 100이고 threshold도 100이므로 모든 픽셀이 1이 된다.

> Test 1은 직접 실행해 PASS를 확인했다. Test 2~5는 예상 결과를 계산해 둔 추가 검증용 테스트이며 제출 전 직접 실행하여 실제 결과와 비교한다.

---

## 10. 시간 복잡도 / 처리량 생각해보기

이미지 크기가 `H × W`라면 전체 픽셀 수는 `H * W`개이다. 처음 RGB를 밝기로 바꿀 때 모든 픽셀을 한 번 확인한다. threshold 계산을 위해 모든 밝기를 한 번 확인하고, 흑백 변환을 위해 다시 모든 밝기를 한 번 확인한다. 반복 횟수는 대략 `3 × H × W`이지만 Big-O에서는 상수 3을 제외하므로 시간 복잡도는 **O(HW)**라고 볼 수 있다. 저장하는 밝기 맵과 결과 역시 픽셀 수에 비례하므로 공간 사용량도 **O(HW)**이다.

---

## 11. 다른 해결 방법

현재 방법은 이해하기 쉽도록 밝기 값을 2차원 리스트에 저장한 후 threshold를 계산하고 다시 흑백 변환을 한다. 다른 방법으로는 모든 밝기를 1차원 리스트에 저장할 수도 있다. 또 전체 합을 입력받는 동시에 계산해 두면 threshold 계산 단계에서 다시 모든 밝기를 순회할 필요가 없다. 다만 0/1 변환을 하려면 threshold가 먼저 필요하므로 각 픽셀의 밝기 정보는 어느 형태로든 저장하거나 다시 계산해야 한다. 초보자가 코드 흐름을 이해하기에는 현재처럼 단계별로 분리한 방식이 더 명확하다고 판단했다.

---

## 12. AI를 사용하면서 배운 점

### 가장 도움이 된 질문 5개
1. RGB 하나의 픽셀은 왜 세 숫자로 표현되는가?
2. `(R + G + B) // 3`은 무엇을 의미하는가?
3. `//`와 `/`의 차이는 무엇인가?
4. `brightness >= threshold`에서 왜 `>=`를 사용하는가?
5. `255,0,0` 같은 문자열을 Python에서 어떻게 숫자로 나누는가?

### 잘못 질문했던 사례
처음에는 문제 전체 코드를 한 번에 완성하려고 하면 각 단계가 왜 필요한지 이해하기 어렵다. 그래서 RGB, 밝기, threshold, 조건 비교처럼 작은 개념 단위로 나누어 확인하는 방식으로 바꾸었다.

### 질문을 바꿨더니 이해가 좋아진 사례
`threshold가 무엇인가?`라고만 묻는 것보다 `왜 전체 밝기의 평균을 threshold로 쓰는가?`라고 질문하니, threshold가 단순한 숫자가 아니라 이미지 전체의 평균 밝기를 기준선으로 삼는 값이라는 점을 이해하기 쉬웠다.

### AI 답변이 애매할 수 있어 직접 확인한 사례
코드가 실행된다고 해서 정답이라고 생각하지 않고 과제 예제 입력을 직접 넣어 `102 / 110 / 100 / 000`이 나오는지 확인했다.

### AI 답변을 직접 검증한 사례
처음 실행할 때 파일 경로 오류가 발생했다. VS Code 탐색기와 터미널 경로를 직접 비교한 뒤 폴더를 이동해 다시 실행했고 정상 출력되는 것을 확인했다.

---

## 13. 마지막 3문장 정리

### 문제 2
처음에는 RGB 세 숫자가 어떻게 하나의 흑백 값으로 바뀌는지와 threshold의 역할이 가장 헷갈렸다. 지금은 각 픽셀의 평균 밝기를 구하고, 전체 밝기의 평균을 기준으로 각 픽셀을 0과 1로 나눈다는 흐름을 이해했다. 다시 풀게 된다면 입력 처리 → 밝기 계산 → threshold 계산 → 0/1 변환 순서로 문제를 나누어 구현하겠다.
