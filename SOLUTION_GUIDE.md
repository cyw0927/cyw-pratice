# SOLUTION GUIDE

> 담당 범위: **문제 2 — 컬러 이미지의 임계값을 계산하고 흑백 이미지 만들기**
>
> 이 문서는 과제 가이드의 13개 필수 항목을 기준으로 문제 2를 자세하게 정리한다. 50개 학습 질문의 상세 설명은 `problem02_learning.ipynb`, AI 질문 과정은 `AI_LOG.md`, 실제 테스트 기록은 `TEST_RESULTS.md`와 함께 확인할 수 있다.

---

## 1. 문제를 내 말로 다시 설명하기

컬러 이미지는 여러 개의 픽셀로 이루어져 있고, 각 픽셀은 R, G, B 세 숫자로 표현된다. 이 문제에서는 RGB 세 값을 그대로 사용하는 대신 `(R + G + B) // 3`으로 평균을 내서 픽셀 하나의 밝기 값으로 바꾼다. 모든 픽셀의 밝기를 구한 다음 그 밝기들을 전부 더한다. 전체 밝기 합을 전체 픽셀 수 `H * W`로 나누면 이미지 전체의 평균 밝기인 `threshold`를 구할 수 있다. 그 뒤 각 픽셀의 밝기가 threshold보다 크거나 같으면 `1`, 작으면 `0`으로 변환한다. 이렇게 하면 컬러 RGB 데이터가 0과 1로만 이루어진 흑백 데이터로 바뀐다. 첫 번째 출력 줄에는 계산된 threshold를 출력하고, 그 아래에는 원래 이미지와 같은 행과 열 구조로 0과 1을 출력한다. 즉 이 문제의 핵심은 **RGB 입력을 읽는 것, 밝기를 계산하는 것, 전체 평균을 구하는 것, 기준과 비교하는 것**이다. 실제 JPG나 PNG 파일을 직접 수정하는 문제가 아니라 숫자로 주어진 이미지 데이터를 처리하는 알고리즘 문제다.

---

## 2. 사용한 핵심 개념

### 리스트
여러 값을 순서대로 저장하는 자료구조다. 이 문제에서는 한 행의 밝기들을 저장하거나, 여러 행의 결과를 저장할 때 사용한다. 예를 들어 `[255, 200, 0]`은 한 행의 세 픽셀 밝기를 나타낼 수 있다.

### 2차원 데이터
리스트 안에 또 다른 리스트가 들어 있는 구조다. 이미지가 행과 열로 이루어져 있으므로 `[[255,200,0],[120,60,30]]`처럼 저장하면 원래 이미지 모양을 유지할 수 있다.

### `for`문
같은 작업을 반복할 때 사용한다. 이미지에는 많은 픽셀이 있으므로 모든 픽셀에 똑같이 밝기 계산과 threshold 비교를 적용하기 위해 반복문이 필요하다.

### 중첩 `for`문
`for`문 안에 또 `for`문이 있는 구조다. 바깥 반복문은 행을 이동하고, 안쪽 반복문은 현재 행의 픽셀들을 하나씩 확인한다.

### `if`문
조건에 따라 다른 결과를 만들 때 사용한다. 이 문제에서는 `brightness >= threshold`이면 `1`, 아니면 `0`을 만든다.

### 함수
특정 기능을 따로 묶어 놓은 코드다. `calc_brightness()`는 한 픽셀의 밝기 계산, `calc_threshold()`는 전체 평균 계산, `to_black_white()`는 0/1 변환을 담당한다.

### 문자열
입력으로 들어오는 `255,0,0`은 처음에는 문자열이다. 또한 최종 결과 `110`도 한 행을 붙여 출력하기 위해 문자열로 만든다.

### `split()`
문자열을 특정 기준으로 나눌 때 사용한다. 한 줄의 픽셀은 공백 기준으로 나누고, 한 픽셀 내부의 R,G,B는 쉼표 기준으로 나눈다.

### `int()`
문자열 숫자를 실제 계산 가능한 정수로 바꾼다. `'255'`는 문자열이고 `255`는 정수이므로 RGB 계산 전에 변환이 필요하다.

### `map()`
여러 값에 같은 함수를 한 번에 적용한다. `map(int, pixel.split(','))`을 사용하면 R,G,B 문자열 세 개를 한 번에 정수로 바꿀 수 있다.

### `append()`
리스트의 맨 뒤에 값을 추가한다. 계산한 밝기를 현재 행에 넣고, 완성된 행을 전체 밝기 맵에 넣을 때 사용한다.

### `in / not in`
이번 최종 코드에서는 직접 사용하지 않지만, 값이 리스트나 문자열 안에 있는지 확인하는 연산자다. 다른 알고리즘 문제에서 중복 확인 등에 자주 사용한다.

### 정수 나눗셈 `//`
나눗셈 결과의 소수점 아래를 버리고 정수 몫만 얻는다. 이 문제에서는 brightness와 threshold를 정수로 계산하기 위해 사용한다.

### 비교 연산자 `>=`
왼쪽 값이 오른쪽 값보다 크거나 같은지 확인한다. 문제 조건이 'threshold 이상이면 1'이므로 `>`가 아니라 `>=`를 사용한다.

---

## 3. 문제 해결을 작은 단계로 나누기

### 문제 2 — 50단계

1. 문제의 최종 출력이 실제 이미지 파일이 아니라 threshold와 0/1 데이터임을 확인한다.
2. 입력 첫 줄에 H와 W 두 숫자가 있다는 것을 확인한다.
3. H가 이미지의 행 수라는 것을 확인한다.
4. W가 한 행의 픽셀 수라는 것을 확인한다.
5. 전체 픽셀 수가 `H * W`라는 것을 확인한다.
6. RGB 한 픽셀이 `R,G,B` 세 값으로 구성된다는 것을 확인한다.
7. 한 줄에서 픽셀과 픽셀이 공백으로 구분된다는 것을 확인한다.
8. 픽셀 내부에서 R,G,B가 쉼표로 구분된다는 것을 확인한다.
9. `input().split()`으로 한 줄의 픽셀 문자열들을 나눈다.
10. 각 픽셀 문자열을 `split(',')`으로 다시 나눈다.
11. 나눈 R,G,B 문자열을 `int()` 또는 `map(int, ...)`으로 정수로 바꾼다.
12. 한 픽셀의 밝기 공식을 `(R + G + B) // 3`으로 확인한다.
13. 빨간색 `(255,0,0)`의 밝기가 85인지 손으로 계산한다.
14. 흰색 `(255,255,255)`의 밝기가 255인지 확인한다.
15. 검은색 `(0,0,0)`의 밝기가 0인지 확인한다.
16. 밝기 계산을 `calc_brightness()` 함수로 분리한다.
17. 이미지 전체 밝기를 저장할 빈 `brightness_map`을 만든다.
18. H번 반복해서 이미지의 각 행을 입력받는다.
19. 각 행을 받을 때 빈 `brightness_row`를 새로 만든다.
20. 현재 행의 픽셀들을 하나씩 반복한다.
21. 각 픽셀의 R,G,B를 분리한다.
22. R,G,B를 정수로 변환한다.
23. `calc_brightness()`로 현재 픽셀 밝기를 구한다.
24. 계산된 brightness를 `brightness_row.append()`로 추가한다.
25. 한 행의 모든 픽셀 처리가 끝났는지 확인한다.
26. 완성된 `brightness_row`를 `brightness_map`에 추가한다.
27. 모든 H개 행 입력이 끝났는지 확인한다.
28. 전체 밝기 합을 저장할 `total = 0`을 만든다.
29. brightness_map의 각 행을 반복한다.
30. 각 행의 brightness를 다시 하나씩 반복한다.
31. `total += brightness`로 전체 밝기 합을 누적한다.
32. 예제 데이터의 total이 920인지 확인한다.
33. 전체 픽셀 수를 `pixel_count = H * W`로 계산한다.
34. `threshold = total // pixel_count`로 전체 평균을 계산한다.
35. 예제에서 `920 // 9 = 102`인지 확인한다.
36. threshold를 첫 줄에 출력한다.
37. 흑백 결과를 저장할 빈 `result` 리스트를 만든다.
38. brightness_map의 각 행을 다시 반복한다.
39. 한 행의 결과 문자열을 만들기 위해 `result_row = ''`로 시작한다.
40. 현재 행의 brightness를 하나씩 확인한다.
41. `brightness >= threshold`인지 비교한다.
42. 조건이 참이면 `result_row` 뒤에 `'1'`을 붙인다.
43. 조건이 거짓이면 `result_row` 뒤에 `'0'`을 붙인다.
44. 한 행의 모든 brightness 비교가 끝났는지 확인한다.
45. 완성된 result_row를 result 리스트에 추가한다.
46. 모든 행의 0/1 변환이 끝났는지 확인한다.
47. result의 각 행을 순서대로 출력한다.
48. 과제 예제 입력으로 threshold 102와 `110 / 100 / 000`이 나오는지 확인한다.
49. 전체 검정, 전체 흰색, 1×1, 경계값 등 추가 테스트를 실행한다.
50. 시간 복잡도, 다른 해결 방법, 시행착오를 정리하고 전체 풀이를 설명할 수 있는지 확인한다.

---

## 4. 수도코드

```text
H, W를 입력받는다
brightness_map을 빈 리스트로 만든다

H번 반복한다
    한 줄의 픽셀 문자열들을 입력받는다
    brightness_row를 빈 리스트로 만든다

    현재 행의 각 픽셀에 대해 반복한다
        픽셀 문자열을 쉼표 기준으로 R, G, B로 나눈다
        R, G, B를 정수로 바꾼다
        brightness = (R + G + B) // 3
        brightness_row에 brightness를 추가한다

    brightness_map에 brightness_row를 추가한다

전체 밝기 합 total을 0으로 만든다
brightness_map의 모든 brightness를 total에 더한다

pixel_count = H * W
threshold = total // pixel_count
threshold를 출력한다

result를 빈 리스트로 만든다
brightness_map의 각 행에 대해 반복한다
    result_row를 빈 문자열로 만든다

    행 안의 각 brightness를 확인한다
        brightness >= threshold 이면 '1'을 붙인다
        아니면 '0'을 붙인다

    완성된 result_row를 result에 추가한다

result의 각 행을 출력한다
```

---

## 5. 최종 코드

```python
# 문제 2. 컬러 이미지의 임계값을 계산하고 흑백 이미지 만들기


def calc_brightness(r, g, b):
    """RGB 한 픽셀의 밝기를 계산한다."""
    return (r + g + b) // 3


def calc_threshold(brightness_map, height, width):
    """전체 이미지의 평균 밝기를 threshold로 계산한다."""
    total = 0

    for row in brightness_map:
        for brightness in row:
            total += brightness

    pixel_count = height * width
    return total // pixel_count


def to_black_white(brightness_map, threshold):
    """각 밝기를 threshold와 비교하여 0/1 이미지로 바꾼다."""
    result = []

    for row in brightness_map:
        result_row = ""

        for brightness in row:
            if brightness >= threshold:
                result_row += "1"
            else:
                result_row += "0"

        result.append(result_row)

    return result


def main():
    # 이미지 높이 H와 너비 W 입력
    height, width = map(int, input().split())

    # 모든 픽셀의 밝기를 원래 행 구조대로 저장
    brightness_map = []

    for _ in range(height):
        pixels = input().split()
        brightness_row = []

        for pixel in pixels:
            # 예: "255,0,0" -> 255, 0, 0
            r, g, b = map(int, pixel.split(","))
            brightness = calc_brightness(r, g, b)
            brightness_row.append(brightness)

        brightness_map.append(brightness_row)

    # 전체 평균 밝기 계산
    threshold = calc_threshold(brightness_map, height, width)
    print(threshold)

    # threshold를 기준으로 0/1 변환
    black_white_image = to_black_white(brightness_map, threshold)

    # 원래 이미지와 같은 행 구조로 출력
    for row in black_white_image:
        print(row)


if __name__ == "__main__":
    main()
```

---

## 6. 코드 한 줄씩 설명하기

### `def calc_brightness(r, g, b):`
한 픽셀의 R, G, B 세 값을 받아서 밝기 하나를 계산하는 함수를 정의한다.

### `return (r + g + b) // 3`
세 색 성분의 합을 3으로 나눈 정수 평균을 반환한다. 예를 들어 `(255,0,0)`은 85가 된다.

### `def calc_threshold(brightness_map, height, width):`
이미지 전체의 평균 밝기, 즉 threshold를 구하는 함수다.

### `total = 0`
모든 밝기를 더하기 위한 누적 변수를 0으로 초기화한다.

### `for row in brightness_map:`
2차원 밝기 맵에서 한 행씩 꺼낸다.

### `for brightness in row:`
현재 행에 들어 있는 각 밝기 값을 하나씩 꺼낸다.

### `total += brightness`
현재 픽셀의 밝기를 전체 합계에 더한다.

### `pixel_count = height * width`
이미지 전체 픽셀 수를 계산한다.

### `return total // pixel_count`
전체 밝기 합을 전체 픽셀 수로 나누어 평균 threshold를 반환한다.

### `def to_black_white(brightness_map, threshold):`
밝기 맵을 threshold와 비교해서 0/1 데이터로 변환하는 함수다.

### `result = []`
변환된 각 행을 저장할 빈 리스트다.

### `result_row = ""`
현재 행의 0과 1을 이어 붙일 빈 문자열이다. 행이 바뀔 때마다 새로 만든다.

### `if brightness >= threshold:`
현재 픽셀 밝기가 threshold 이상인지 확인한다. 문제에서 '이상'이라고 했으므로 `>=`를 사용한다.

### `result_row += "1"`
밝기가 기준 이상이면 현재 행 결과 뒤에 1을 붙인다.

### `result_row += "0"`
밝기가 기준 미만이면 현재 행 결과 뒤에 0을 붙인다.

### `result.append(result_row)`
한 행의 변환이 끝났으므로 전체 결과 리스트에 추가한다.

### `return result`
완성된 0/1 이미지의 모든 행을 반환한다.

### `height, width = map(int, input().split())`
첫 입력 줄을 공백 기준으로 나누고 두 값을 정수로 바꿔 height와 width에 저장한다.

### `brightness_map = []`
이미지 전체의 밝기 데이터를 저장할 빈 2차원 리스트를 준비한다.

### `for _ in range(height):`
이미지 높이 H만큼 한 행씩 입력받는다. 반복 횟수만 필요하므로 변수 이름으로 `_`를 사용한다.

### `pixels = input().split()`
현재 입력 줄을 공백 기준으로 나눠 픽셀 문자열 리스트로 만든다.

### `brightness_row = []`
현재 행의 밝기만 저장할 빈 리스트를 만든다.

### `for pixel in pixels:`
현재 행의 픽셀 문자열들을 하나씩 처리한다.

### `r, g, b = map(int, pixel.split(','))`
`255,0,0` 같은 문자열을 쉼표로 나누고 세 값을 정수로 변환한다.

### `brightness = calc_brightness(r, g, b)`
현재 픽셀의 밝기를 계산한다.

### `brightness_row.append(brightness)`
계산된 밝기를 현재 행 리스트에 저장한다.

### `brightness_map.append(brightness_row)`
한 행 처리가 끝났으므로 전체 밝기 맵에 행을 추가한다.

### `threshold = calc_threshold(...)`
모든 밝기의 평균을 계산해서 threshold를 얻는다.

### `print(threshold)`
문제 조건에 따라 threshold를 첫 번째 출력 줄에 출력한다.

### `black_white_image = to_black_white(...)`
밝기 맵을 0과 1로 변환한다.

### `for row in black_white_image:` / `print(row)`
완성된 0/1 결과를 원래 이미지와 같은 행 구조로 한 줄씩 출력한다.

### `if __name__ == "__main__":`
이 파일을 직접 실행했을 때만 main 함수를 실행하도록 한다.

---

## 7. 예제 데이터 직접 추적

### 입력

```text
3 3
255,255,255 200,200,200 0,0,0
120,120,120 60,60,60 30,30,30
255,0,0 0,255,0 0,0,255
```

### RGB → brightness 변환

| 단계 | 위치 | RGB | 계산 | brightness |
|---:|---|---|---|---:|
| 1 | 1행 1열 | 255,255,255 | 765 // 3 | 255 |
| 2 | 1행 2열 | 200,200,200 | 600 // 3 | 200 |
| 3 | 1행 3열 | 0,0,0 | 0 // 3 | 0 |
| 4 | 2행 1열 | 120,120,120 | 360 // 3 | 120 |
| 5 | 2행 2열 | 60,60,60 | 180 // 3 | 60 |
| 6 | 2행 3열 | 30,30,30 | 90 // 3 | 30 |
| 7 | 3행 1열 | 255,0,0 | 255 // 3 | 85 |
| 8 | 3행 2열 | 0,255,0 | 255 // 3 | 85 |
| 9 | 3행 3열 | 0,0,255 | 255 // 3 | 85 |

따라서 `brightness_map`은 다음과 같다.

```text
[
  [255, 200,   0],
  [120,  60,  30],
  [ 85,  85,  85]
]
```

### threshold 계산 추적

| 단계 | 변수 | 값 | 설명 |
|---:|---|---:|---|
| 1 | total | 920 | 9개 brightness의 합 |
| 2 | height | 3 | 행 수 |
| 3 | width | 3 | 한 행 픽셀 수 |
| 4 | pixel_count | 9 | 3 × 3 |
| 5 | threshold | 102 | 920 // 9 |

### threshold 비교 추적

| 위치 | brightness | threshold | 비교 | 결과 |
|---|---:|---:|---|---:|
| 1행 1열 | 255 | 102 | 255 >= 102 | 1 |
| 1행 2열 | 200 | 102 | 200 >= 102 | 1 |
| 1행 3열 | 0 | 102 | 0 < 102 | 0 |
| 2행 1열 | 120 | 102 | 120 >= 102 | 1 |
| 2행 2열 | 60 | 102 | 60 < 102 | 0 |
| 2행 3열 | 30 | 102 | 30 < 102 | 0 |
| 3행 1열 | 85 | 102 | 85 < 102 | 0 |
| 3행 2열 | 85 | 102 | 85 < 102 | 0 |
| 3행 3열 | 85 | 102 | 85 < 102 | 0 |

### 최종 출력

```text
102
110
100
000
```

---

## 8. 오류와 시행착오

### 오류 1. `problem02.py`를 찾을 수 없다는 오류
- **발생 내용:** `can't open file ... problem02.py: [Errno 2] No such file or directory`
- **원인:** 터미널 현재 위치와 실제 파일 위치가 달랐다.
- **원인을 찾은 방법:** VS Code Explorer의 파일 위치와 PowerShell 프롬프트의 현재 경로를 비교했다.
- **AI에게 한 질문:** 왜 파일이 보이는데 Python에서는 파일을 못 찾는지 질문했다.
- **수정:** 올바른 폴더로 이동하거나 저장소 구조를 루트에 파일이 바로 보이도록 단순화했다.
- **배운 점:** 실행 명령어뿐 아니라 현재 작업 경로도 확인해야 한다.

### 오류 2. `cd`와 `python` 명령을 한 줄에 붙여 입력함
- **발생 내용:** PowerShell이 `python ...` 부분까지 `cd`의 인수로 해석하여 `Set-Location` 오류가 발생했다.
- **원인:** `cd ...`와 `python ...` 사이에 엔터나 명령 구분자가 없었다.
- **원인을 찾은 방법:** 터미널에 입력된 명령 전체를 확인했다.
- **AI에게 한 질문:** 왜 `Set-Location` 오류가 뜨는지 질문했다.
- **수정:** `cd` 명령을 먼저 실행하고 다음 줄에서 `python problem02.py`를 실행했다.
- **배운 점:** PowerShell에서 명령 단위를 분리해야 한다.

### 오류 3. `>`와 `>=`를 혼동할 가능성
- **발생 내용:** threshold와 정확히 같은 brightness가 있을 때 결과가 달라질 수 있다.
- **원인:** '이상'과 '초과'의 차이를 코드에서 놓칠 수 있다.
- **원인을 찾은 방법:** `102 >= 102`와 `102 > 102`를 직접 비교했다.
- **AI에게 한 질문:** threshold와 같은 값은 0인지 1인지 질문했다.
- **수정:** 문제 조건대로 `brightness >= threshold`를 사용했다.
- **배운 점:** 경계 조건은 직접 작은 값으로 테스트해야 한다.

### 오류 4. 저장소를 여러 폴더 안에 중첩해서 clone함
- **발생 내용:** `cyw-pratice-main/image_algorithm_quiz/cyw-pratice`처럼 폴더가 겹쳐 최신 파일과 예전 ZIP 파일을 혼동했다.
- **원인:** 기존 ZIP 폴더 안에서 다시 `git clone`을 실행했다.
- **원인을 찾은 방법:** Explorer에 같은 이름의 폴더가 여러 단계로 보이는 것을 확인했다.
- **수정:** GitHub 저장소 파일을 루트로 이동하고 빈 상위 폴더에서 다시 clone했다.
- **배운 점:** clone은 작업용 상위 폴더에서 한 번만 하고 이후에는 `git pull`을 사용한다.

---

## 9. 테스트 케이스

| Test | 입력 요약 | 예상 결과 | 실제 결과 | PASS/FAIL |
|---|---|---|---|---|
| 1 | 과제 3×3 기본 예제 | threshold 102 / 110 / 100 / 000 | 동일 | PASS |
| 2 | 모든 픽셀이 0,0,0 | threshold 0 / 모든 결과 1 | 동일 | PASS |
| 3 | 모든 픽셀이 255,255,255 | threshold 255 / 모든 결과 1 | 동일 | PASS |
| 4 | 1×1, 픽셀 255,0,0 | threshold 85 / 결과 1 | 동일 | PASS |
| 5 | 밝고 어두운 픽셀이 섞인 입력 | 평균 이상 1, 미만 0 | 동일 | PASS |

### Test 1 — 기본 예제

```text
3 3
255,255,255 200,200,200 0,0,0
120,120,120 60,60,60 30,30,30
255,0,0 0,255,0 0,0,255
```

예상/실제:

```text
102
110
100
000
```

### Test 2 — 전체 검정
모든 brightness가 0이라 threshold도 0이다. 조건이 `>=`라서 `0 >= 0`이 참이므로 결과는 모두 1이다. 직관과 다르지만 문제 규칙에는 맞는다.

### Test 3 — 전체 흰색
모든 brightness가 255이고 threshold도 255다. 같은 값은 `>=`에 포함되므로 모두 1이다.

### Test 4 — 1×1
한 픽셀의 밝기가 곧 평균이므로 threshold와 같다. 따라서 결과는 1이다.

### Test 5 — 경계/혼합 입력
threshold보다 큰 값, 작은 값, 정확히 같은 값을 섞어서 `>=` 조건이 정확히 작동하는지 확인한다.

---

## 10. 시간 복잡도 / 처리량 생각해보기

처음에는 '이미지가 커지면 픽셀을 더 많이 확인해야 하므로 처리 시간이 늘어난다'고 생각할 수 있다. 이를 Big-O로 표현하면, 픽셀 수는 `H * W`다. 밝기 계산 과정에서 모든 픽셀을 한 번 확인하고, threshold 합계 계산에서 다시 확인하고, 흑백 변환에서 다시 확인한다. 즉 대략 `3 × H × W` 정도의 작업이지만 Big-O에서는 상수 3을 생략한다.

따라서 시간 복잡도는 다음과 같이 정리할 수 있다.

```text
O(H × W)
```

픽셀 수를 N이라고 부르면 `O(N)`이라고도 할 수 있다. 이미지의 가로 또는 세로 크기가 커지면 전체 픽셀 수에 비례해서 처리량이 증가한다.

공간 복잡도는 brightness_map과 결과를 저장하므로 역시 대략 `O(H × W)`다.

---

## 11. 다른 해결 방법

### 방법 1. 입력하면서 total도 같이 누적하기
현재 코드는 brightness_map을 만든 뒤 다시 순회해서 total을 구한다. 다른 방법으로는 RGB를 brightness로 바꿀 때 바로 `total += brightness`도 같이 실행할 수 있다. 그러면 전체 합을 구하기 위한 별도 순회 하나를 줄일 수 있다.

### 방법 2. 1차원 리스트 사용
모든 brightness를 한 줄 리스트에 저장하고 인덱스를 이용해 다시 행별로 출력할 수도 있다. 하지만 초보자 입장에서는 원래 이미지 구조가 바로 보이는 2차원 리스트가 더 이해하기 쉽다.

### 방법 3. 실제 이미지 처리에서 다른 밝기 공식 사용
실제 영상 처리에서는 사람 눈의 민감도를 고려해서 R,G,B에 서로 다른 가중치를 주는 밝기 공식도 사용한다. 하지만 이 과제에서는 `(R+G+B)//3`을 사용하라고 명시했으므로 과제 코드에서는 바꾸면 안 된다.

### 방법 4. 다른 threshold 방식
고정 threshold 128, 중앙값, Otsu 방식 등 다양한 방법이 존재한다. 이것 역시 실제 이미지 처리의 다른 접근일 뿐, 이번 과제의 정답 규칙은 **전체 평균 밝기**다.

---

## 12. AI를 사용하면서 배운 점

### 가장 도움이 된 질문 5개
1. 이 문제에서 최종적으로 원하는 결과가 무엇인지 질문한 것.
2. `RGB → brightness → threshold → 0/1` 흐름을 단계별로 질문한 것.
3. `/`와 `//`가 왜 다른지 질문한 것.
4. threshold와 같은 brightness가 왜 1인지 질문한 것.
5. 예제의 102 / 110 / 100 / 000이 어떻게 나오는지 중간값을 추적한 것.

### 잘못 질문했던 사례
처음에는 이 프로그램을 실행하면 실제 이미지 파일이 밝아지는 것인지 질문했다. 문제에서 사용하는 brightness라는 단어 때문에 이미지 보정 문제로 오해했다.

### 질문을 바꿨더니 이해가 좋아진 사례
처음에는 '코드를 만들어 달라'는 식으로 접근했지만, 이후 '255,0,0의 밝기는 왜 85인가?', '920에서 102가 왜 나오는가?'처럼 중간 계산을 하나씩 질문하니 전체 흐름을 이해하기 쉬워졌다.

### AI 답변이 틀리거나 애매했던 사례
전체 검정 이미지의 결과를 처음 보면 '검정이니까 0이어야 하지 않나?'라고 생각하기 쉽다. 그러나 문제 규칙은 실제 색을 그대로 표시하는 것이 아니라 threshold 이상 여부를 표시하므로 `0 >= 0`이 참이 되어 1이 나온다. 이 부분은 직관보다 문제 조건을 직접 계산해서 확인해야 했다.

### AI 답변을 직접 검증한 사례
과제 예제 입력을 실제 `problem02.py`에 넣어 threshold가 102인지, 결과가 `110 / 100 / 000`인지 직접 실행했다. 또한 `102 >= 102`와 `102 > 102`를 작은 코드로 비교해 경계 조건도 확인했다.

### AI 사용에서 배운 점
AI가 최종 답을 바로 주는 것보다, 내가 이해하지 못한 한 단계를 질문하고 직접 작은 값으로 검산하는 방식이 더 도움이 됐다. 코드가 작동하는 것과 코드를 설명할 수 있는 것은 다르다는 것도 알게 됐다.

---

## 13. 마지막 3문장 정리

1. **처음에는 무엇이 어려웠는가?** RGB 세 값이 어떻게 하나의 밝기가 되고, 그 밝기들이 다시 threshold와 0/1 결과로 연결되는지 전체 흐름이 가장 어려웠다.
2. **지금은 무엇을 이해했는가?** 입력 문자열을 분리해 RGB를 읽고, brightness를 계산하고, 전체 평균 threshold를 구한 뒤 조건문으로 흑백 데이터를 만드는 과정을 설명할 수 있다.
3. **다시 풀면 어떻게 접근할 것인가?** 최종 코드를 한 번에 작성하지 않고 입력 처리, 밝기 계산, 평균 계산, 조건 비교, 출력, 테스트를 작은 단계로 나누어 하나씩 직접 확인하면서 구현할 것이다.
