# AI 학습 기록 — 문제 2

> 담당 범위: 문제 2 `컬러 이미지의 임계값을 계산하고 흑백 이미지 만들기`
>
> 아래 내용은 `problem02_learning.ipynb`의 Q01~Q75와 맞춰서 다시 정리한 학습 기록이다. 실제 대화 원문을 그대로 복사한 것은 아니고, 노트북에서 확인한 내용을 **처음 Python을 배우는 입장에서 이해한 순서대로 다시 적은 것**이다.

---

## Q01. 이 문제는 결국 뭘 하라는 거지?

### 내가 궁금했던 것
처음 문제를 봤을 때는 컬러 이미지를 흑백으로 바꾸라고 해서 진짜 사진 파일을 열어서 바꾸는 건 줄 알았다. 그런데 입력 예제를 보니까 사진 파일은 없고 숫자만 있었다. 그래서 이게 정확히 뭘 하라는 문제인지부터 물어봤다.

### 알게 된 것
사진 파일을 직접 건드리는 게 아니라 RGB 숫자를 받아서 각 픽셀의 밝기를 계산하고, 전체 평균보다 밝은지 어두운지를 1과 0으로 표시하는 문제였다.

### 내가 확인한 것
```python
print("RGB -> 밝기 -> 평균 -> 0/1")
```

처음에는 이 네 단계만 기억하기로 했다. 코드를 보기 전에 순서부터 이해하는 게 낫다고 생각했다.

---

## Q02. RGB가 뭐지?

### 내가 궁금했던 것
`255,0,0` 이런 숫자가 계속 나오는데 왜 숫자가 세 개인지 몰랐다.

### 알게 된 것
RGB는 Red, Green, Blue이고 픽셀 하나를 세 숫자로 표현한다. 숫자 세 개가 픽셀 세 개라는 뜻이 아니라 픽셀 하나 안에 빨강, 초록, 파랑 값이 각각 들어 있는 것이다.

### 직접 확인
```python
red = (255, 0, 0)
green = (0, 255, 0)
blue = (0, 0, 255)

print(red)
print(green)
print(blue)
```

이걸 보고 괄호 안의 숫자 세 개가 한 세트라는 걸 이해했다.

---

## Q03. 왜 0부터 255까지만 쓰지?

### 내가 궁금했던 것
RGB 예제를 보면 255가 자꾸 최대값처럼 나오길래 이유가 궁금했다.

### 알게 된 것
8비트로 한 색을 표현하면 경우의 수가 256개라서 0부터 시작하면 255까지 나온다고 했다.

### 직접 확인
```python
print(2 ** 8)
print(2 ** 8 - 1)
```

결과가 256, 255가 나왔다. 처음에는 256개면 256까지 아닌가 했는데 0도 하나로 세기 때문에 마지막이 255였다.

---

## Q04. `255,0,0`은 숫자인가 문자열인가?

### 내가 궁금했던 것
화면에는 숫자로 보이는데 `input()`으로 받으면 바로 계산 가능한 건지 헷갈렸다.

### 알게 된 것
`input()`으로 받으면 처음에는 문자열이다. 그래서 쉼표를 기준으로 나누고 다시 숫자로 바꿔야 한다.

### 직접 확인
```python
pixel = "255,0,0"
print(pixel)
print(type(pixel))
print(pixel.split(','))
```

`split()`을 하고 나서도 안쪽 값들이 문자열이라는 것도 같이 확인했다.

---

## Q05. 검은색하고 흰색은 어떻게 표시하지?

### 내가 궁금했던 것
가장 어두운 색과 가장 밝은 색을 먼저 계산해 보면 밝기 공식이 이해하기 쉬울 것 같았다.

### 알게 된 것
검정은 `(0,0,0)`, 흰색은 `(255,255,255)`이다. 이 문제의 밝기 공식으로 계산하면 각각 0과 255가 된다.

### 직접 확인
```python
black = (0, 0, 0)
white = (255, 255, 255)

print(sum(black) // 3)
print(sum(white) // 3)
```

흰색은 합이 765인데 밝기는 평균이라서 255라는 점이 처음에는 조금 헷갈렸다.

---

## Q06. 왜 RGB 세 개를 굳이 밝기 하나로 바꾸지?

### 내가 궁금했던 것
RGB 값이 이미 있는데 그냥 그걸 쓰면 안 되나 싶었다.

### 알게 된 것
세 숫자를 그대로 비교하는 것보다 밝기라는 숫자 하나로 만들면 모든 픽셀을 같은 기준하고 비교하기 쉽다.

### 직접 확인
```python
rgb1 = (255, 0, 0)
rgb2 = (85, 85, 85)

b1 = sum(rgb1) // 3
b2 = sum(rgb2) // 3

print(b1, b2)
```

둘은 색은 다른데 밝기는 둘 다 85가 나왔다. 여기서 색과 밝기는 같은 개념이 아니라는 걸 알았다.

---

## Q07. 밝기 공식은 정확히 뭐였지?

### 내가 궁금했던 것
계산식을 코드로 어떻게 써야 하는지 확인했다.

### 알게 된 것
이 문제에서는 그냥 세 값을 더해서 3으로 나눈다.

```python
brightness = (r + g + b) // 3
```

### 직접 확인
```python
r = 120
g = 120
b = 120
brightness = (r + g + b) // 3
print(brightness)
```

120이 그대로 나왔다. 문제에 공식이 정해져 있으니 다른 공식은 찾아서 쓰지 않기로 했다.

---

## Q08. `/`하고 `//`는 뭐가 다르지?

### 내가 궁금했던 것
나눗셈이면 `/`만 알고 있었는데 문제에는 `//`가 나왔다.

### 알게 된 것
`/`는 소수까지 나오고 `//`는 몫만 남긴다.

### 직접 확인
```python
print(7 / 3)
print(7 // 3)
print(type(7 / 3))
print(type(7 // 3))
```

`2.333...`하고 `2`가 나왔다. 이 문제에서는 정수로 계산해야 해서 `//`를 쓴다.

---

## Q09. 빨간색이 왜 밝기 85지?

### 내가 궁금했던 것
`255,0,0`이면 255가 제일 큰 숫자인데 왜 밝기가 85인지 바로 감이 안 왔다.

### 알게 된 것
세 값을 다 더해서 3으로 나누니까 `255 // 3 = 85`가 된다.

### 직접 확인
```python
colors = [
    (255, 0, 0),
    (0, 255, 0),
    (0, 0, 255)
]

for rgb in colors:
    print(rgb, sum(rgb) // 3)
```

세 색이 전부 85가 나왔다. 실제 색은 다르지만 이 문제의 계산에서는 같은 밝기로 처리된다.

---

## Q10. 이 계산을 함수로 만드는 이유가 있나?

### 내가 궁금했던 것
한 줄짜리 계산인데 굳이 함수를 만들어야 하나 싶었다.

### 알게 된 것
픽셀마다 똑같은 계산을 계속 쓰니까 함수로 만들어 두면 여러 번 불러 쓰기 편하고 나중에 확인하기도 쉽다.

### 직접 확인
```python
def calc_brightness(r, g, b):
    return (r + g + b) // 3

print(calc_brightness(0, 0, 0))
print(calc_brightness(255, 255, 255))
print(calc_brightness(255, 0, 0))
```

함수라는 게 거창한 게 아니라 반복해서 쓸 계산을 이름 붙여 놓는 거라고 이해했다.

---

## Q11. H랑 W는 뭐지?

### 내가 궁금했던 것
입력 첫 줄에 `3 3`이 있는데 뭘 뜻하는지부터 확인했다.

### 알게 된 것
H는 세로 행 개수, W는 한 행에 있는 픽셀 개수다.

### 직접 확인
```python
H = 3
W = 4
print("행:", H)
print("열:", W)
print("전체 칸:", H * W)
```

3행 4열이면 12칸이라는 식으로 생각하니까 쉬웠다.

---

## Q12. 전체 픽셀 수는 왜 H * W지?

### 내가 궁금했던 것
평균을 내려고 보니 전체 픽셀 수가 필요했다.

### 알게 된 것
한 줄에 W개씩 있고 그런 줄이 H개 있으니까 곱하면 된다.

### 직접 확인
```python
H = 2
W = 5
pixel_count = H * W
print(pixel_count)
```

`H + W`가 아니라 곱셈이라는 것만 기억하면 된다.

---

## Q13. `3 3`을 어떻게 변수 두 개에 넣지?

### 내가 궁금했던 것
`input()` 한 번으로 숫자 두 개를 받아야 했다.

### 처음에는 이렇게 쪼개서 봤다
```python
line = "3 3"
parts = line.split()
print(parts)
```

결과는 `['3', '3']`이었다. 그런데 문자열이라서 다시 숫자로 바꿔야 했다.

### 그다음 해본 것
```python
height, width = map(int, "3 3".split())
print(height)
print(width)
print(type(height))
```

여기서 `map(int, ...)`가 문자열 여러 개를 한 번에 정수로 바꾸는 데 쓰인다는 걸 알았다.

---

## Q14. 한 줄에 RGB가 여러 개 붙어 있으면 어떻게 나누지?

### 내가 궁금했던 것
예제 한 줄이 이렇게 생겼다.

```text
255,255,255 200,200,200 0,0,0
```

쉼표도 있고 공백도 있어서 처음에는 `split(',')`을 먼저 해야 하나 헷갈렸다.

### 알게 된 것
먼저 공백으로 픽셀끼리 나눈다.

### 직접 확인
```python
row = "255,255,255 200,200,200 0,0,0"

pixels = row.split()

print("원래 문자열:")
print(row)

print("공백으로 나눈 결과:")
print(pixels)

print("픽셀 개수:")
print(len(pixels))

for pixel in pixels:
    print("픽셀 하나:", pixel)
```

결과가 RGB 문자열 세 개로 나뉘었다. **공백은 픽셀 사이를 나누는 것**이라고 기억했다.

---

## Q15. 그러면 픽셀 하나 안의 RGB는 어떻게 나누지?

### 내가 궁금했던 것
Q14에서 픽셀 단위로 나눴더니 여전히 `255,255,255` 같은 문자열이었다.

### 알게 된 것
이번에는 쉼표를 기준으로 한 번 더 나눈다.

### 직접 확인
```python
pixel = "255,0,0"
parts = pixel.split(',')

print(pixel)
print(parts)
print("몇 조각인지:", len(parts))

for part in parts:
    print(part, type(part))
```

이렇게 해보니 공백 `split()`하고 쉼표 `split(',')`의 역할이 완전히 다르다는 걸 알았다.

---

## Q16. 문자열 상태로 더하면 안 되나?

### 내가 궁금했던 것
`'255'`도 숫자처럼 생겼으니까 그냥 더해도 될 것 같았다.

### 직접 해본 것
```python
a = "2"
b = "3"

print(a + b)
print(int(a) + int(b))
```

결과가 `23`, `5`였다.

### 알게 된 것
문자열에서 `+`는 숫자 덧셈이 아니라 글자를 붙이는 것이다. 그래서 RGB 계산 전에는 `int()`로 바꿔야 한다.

---

## Q17. RGB 세 개를 한 번에 int로 바꿀 수 있나?

### 내가 궁금했던 것
`int(parts[0])`, `int(parts[1])`, `int(parts[2])`를 전부 쓰는 건 귀찮아 보였다.

### 알게 된 것
`map(int, ...)`를 사용하면 된다.

### 직접 확인
```python
pixel = "255,0,0"
r, g, b = map(int, pixel.split(','))

print(r, g, b)
print(type(r), type(g), type(b))
```

이 한 줄이 처음에는 복잡해 보였는데 `split → int 변환 → r,g,b에 넣기` 세 단계가 합쳐진 것이라고 생각하니까 이해됐다.

---

## Q18. for문을 왜 두 번 써야 하지?

### 내가 궁금했던 것
코드를 보니까 `for` 안에 또 `for`가 있었다. 하나로 못 하나 싶었다.

### 알게 된 것
이미지가 2차원이라서 첫 번째 for문은 줄을 하나씩 보고, 두 번째 for문은 그 줄 안의 픽셀을 하나씩 본다.

### 작은 숫자로 먼저 확인
```python
numbers = [
    [1, 2, 3],
    [4, 5, 6]
]

for row in numbers:
    print("현재 행:", row)
    for value in row:
        print("  현재 값:", value)
```

### RGB에도 적용해 보기
```python
rows = [
    ["255,255,255", "200,200,200", "0,0,0"],
    ["120,120,120", "60,60,60", "30,30,30"]
]

for row in rows:
    print("한 줄 시작")
    for pixel in row:
        r, g, b = map(int, pixel.split(','))
        brightness = (r + g + b) // 3
        print(pixel, "->", brightness)
```

직접 출력해보니 바깥 for와 안쪽 for가 각각 뭘 도는지 보였다.

---

## Q19. `brightness_row`는 왜 매번 빈 리스트로 만들지?

### 내가 궁금했던 것
그냥 brightness를 한 리스트에 다 넣으면 안 되나 싶었다.

### 알게 된 것
원래 이미지가 행별로 나뉘어 있으니까 밝기도 행별로 보관하려고 매 줄마다 새 리스트를 만든다.

### 직접 확인
```python
rows = [
    [255, 200, 0],
    [120, 60, 30]
]

brightness_map = []

for row in rows:
    brightness_row = []

    for value in row:
        brightness_row.append(value)

    print("이번 행:", brightness_row)
    brightness_map.append(brightness_row)

print("전체:", brightness_map)
```

`brightness_row = []`의 위치가 중요했다. 새 행이 시작될 때 다시 비워줘야 행끼리 안 섞인다.

---

## Q20. append는 정확히 뭐였지?

### 내가 궁금했던 것
리스트 뒤에 값을 추가하는 문법이라고만 알고 있었는데 이 문제에서는 두 번 사용됐다.

### 직접 확인
```python
brightness_row = []

brightness_row.append(255)
print(brightness_row)

brightness_row.append(200)
print(brightness_row)

brightness_row.append(0)
print(brightness_row)
```

### 이 문제에서는
```python
brightness_map = []
brightness_row = [255, 200, 0]
brightness_map.append(brightness_row)
print(brightness_map)
```

한 번은 **픽셀 밝기를 한 행에 넣고**, 또 한 번은 **완성된 행을 전체 이미지에 넣는 것**이었다.

---

## Q21. 왜 굳이 2차원 리스트로 저장하지?

### 내가 궁금했던 것
밝기 값 9개를 그냥 `[255,200,0,120,...]`처럼 한 줄에 저장해도 계산은 될 것 같았다.

### 알게 된 것
평균만 구할 거면 한 줄 리스트도 가능하지만 최종 결과를 다시 행별로 출력해야 해서 2차원으로 가지고 있는 게 훨씬 알아보기 쉽다.

### 직접 확인
```python
brightness_map = [
    [255, 200, 0],
    [120, 60, 30],
    [85, 85, 85]
]

print(brightness_map)
print("2번째 행:", brightness_map[1])
print("2번째 행 첫 값:", brightness_map[1][0])
```

처음에는 `[1][0]` 같은 인덱스가 헷갈렸는데 첫 번째 숫자가 행, 두 번째 숫자가 그 행 안의 위치라고 생각했다.

---

## Q22. 합계를 구할 때 왜 total = 0부터 시작하지?

### 내가 궁금했던 것
반복문에서 합계를 구할 때 항상 `total = 0`을 먼저 쓰는 이유가 궁금했다.

### 알게 된 것
아직 아무 숫자도 더하지 않은 상태의 합이 0이기 때문이다. 그다음 값을 하나씩 더한다.

### 직접 확인
```python
values = [255, 200, 0]
total = 0

print("처음 total:", total)

for value in values:
    total = total + value
    print(value, "더한 뒤:", total)
```

출력이 255 → 455 → 455가 되는 걸 보고 누적되는 과정을 이해했다.

---

## Q23. `total += brightness`는 또 뭐지?

### 내가 궁금했던 것
`+=`가 익숙하지 않아서 풀어서 써봤다.

### 직접 비교
```python
total1 = 10
total1 = total1 + 5

print(total1)


total2 = 10
total2 += 5

print(total2)
```

둘 다 15였다.

### 알게 된 것
```python
total += brightness
```
은
```python
total = total + brightness
```
을 줄여 쓴 것이다. 처음에는 긴 형태로 생각하는 게 더 이해하기 쉬웠다.

---

## Q24. threshold가 도대체 뭐지?

### 내가 궁금했던 것
문제 설명에서 threshold라는 영어가 계속 나오는데 그냥 무슨 특별한 값인 줄 알았다.

### 알게 된 것
이 문제에서 threshold는 **모든 픽셀 밝기의 평균**이다. 말이 어려워 보여도 그냥 평균값이다.

### 직접 확인
```python
brightness_map = [
    [255, 200, 0],
    [120, 60, 30],
    [85, 85, 85]
]

total = 0

for row in brightness_map:
    for brightness in row:
        total += brightness

pixel_count = 3 * 3
threshold = total // pixel_count

print("전체 합:", total)
print("픽셀 수:", pixel_count)
print("threshold:", threshold)
```

여기까지 하고 나니까 threshold가 갑자기 어려운 용어처럼 보이지 않았다.

---

## Q25. 왜 예제 threshold가 102가 나오지?

### 내가 궁금했던 것
정답에 102가 딱 나와 있는데 어디서 나온 숫자인지 직접 따라가 봤다.

### 밝기 값부터 다시 적어봄
```python
brightness_map = [
    [255, 200, 0],
    [120, 60, 30],
    [85, 85, 85]
]
```

### 하나씩 더해 보기
```python
total = 0

for row in brightness_map:
    print("현재 행:", row)

    for brightness in row:
        total += brightness
        print("  더한 값:", brightness, "/ 현재 합:", total)

print("최종 합:", total)
```

최종 합이 920이었다.

### 평균 계산
```python
H = 3
W = 3
pixel_count = H * W
threshold = 920 // pixel_count

print("픽셀 수:", pixel_count)
print("920 // 9 =", threshold)
```

결과가 102였다. `/`로 계산하면 102.22 정도인데 문제에서는 `//`를 쓰기 때문에 102만 남는다.

---

# Q26~Q75 — 다음 학습 단계

아래부터는 앞에서 이해한 입력 처리와 밝기 계산을 이용해 threshold 계산, 0/1 변환, 함수화, 테스트와 실행환경까지 이어서 확인한 내용이다. Q01~Q25보다 뒤쪽이라 표현은 조금 짧게 정리했지만 같은 순서로 노트북 코드와 맞춰 확인한다.

---

## Q26. 전체 밝기 합은 왜 중첩 for문으로 구하지?
2차원 리스트라서 행을 먼저 보고 그 안의 값을 다시 봐야 했다. `for row in brightness_map:` 안에서 `for brightness in row:`를 돌리면 모든 값을 빠짐없이 볼 수 있었다.

## Q27. threshold 함수에 height, width가 왜 필요하지?
전체 픽셀 수를 `height * width`로 계산하기 위해서였다. 밝기 값하고 이미지 크기는 서로 다른 정보라는 점도 알게 됐다.

## Q28. 픽셀 수를 len으로 구해도 되나?
가능했다. 다만 이 문제는 H와 W를 입력으로 주니까 `height * width`가 문제 설명과 바로 연결돼서 더 보기 쉬웠다.

## Q29. threshold를 왜 먼저 print하지?
계산 순서 때문이 아니라 **출력 형식이 그렇게 정해져 있기 때문**이었다. 알고리즘 문제는 출력 순서까지 맞아야 한다.

## Q30. 결과를 왜 문자열로 만들지?
`110`처럼 공백 없이 붙여 출력해야 해서 문자열이 편했다. `[1, 1, 0]`을 그대로 출력하면 문제에서 원하는 모양이 아니었다.

## Q31. result_row를 왜 행마다 다시 빈 문자열로 만들지?
안 그러면 첫 행의 `110` 뒤에 다음 행 `100`이 붙어서 `110100`처럼 되어버린다. 행마다 새로 시작해야 했다.

## Q32. brightness가 threshold 이상이면 왜 1이지?
문제에서 그렇게 정했기 때문이다. 여기서 1은 원래 흰색이라는 뜻보다는 평균 이상이라는 표시라고 이해했다.

## Q33. 왜 `>` 말고 `>=`지?
'이상'은 같은 값도 포함한다. `100 > 100`은 False지만 `100 >= 100`은 True였다.

## Q34. result.append(result_row)는 왜 하나?
한 행이 완성되면 그 행을 전체 결과 리스트에 넣기 위해서였다. append 위치가 안쪽 for문이면 행이 이상하게 여러 번 추가될 수 있다.

## Q35. to_black_white 함수는 뭘 받지?
`brightness_map`하고 `threshold`를 받고 `['110','100','000']` 같은 결과를 돌려준다. RGB는 이미 앞 단계에서 밝기로 바뀌었기 때문에 이 함수에서는 필요 없었다.

## Q36. main 함수는 왜 있지?
입력 받고, 계산 함수 부르고, 출력하는 순서를 한곳에 모으는 역할이었다. 처음에는 함수가 많아져서 더 복잡해 보였는데 역할별로 나눠 보면 오히려 찾기 쉬웠다.

## Q37. `if __name__ == '__main__'`은 뭔가?
이 파일을 직접 실행할 때 main을 실행시키는 코드였다. 완전히 익숙하지는 않지만 Python 파일 맨 아래에서 자주 쓰는 구조라는 건 알았다.

## Q38. 예제 입력을 전부 밝기로 바꾸면?
`[[255,200,0],[120,60,30],[85,85,85]]`가 된다. 특히 빨강, 초록, 파랑이 전부 85가 되는 걸 다시 확인했다.

## Q39. 첫 번째 결과가 왜 110이지?
255와 200은 102 이상이라 1, 0은 102 미만이라 0이다. 그래서 순서대로 붙이면 110이다.

## Q40. 전체 검정인데 왜 결과가 11/11이지?
이 부분이 제일 이상하게 느껴졌다. 모두 밝기 0이면 평균도 0이라서 `0 >= 0`이 전부 참이 된다. 문제 조건대로라면 맞는 결과였다.

## Q41. 전체 흰색은?
모든 밝기가 255고 평균도 255라서 전부 같은 값이다. `>=`라서 전부 1이 된다.

## Q42. 1x1 이미지는 왜 항상 1이지?
픽셀이 하나면 그 값 자체가 평균이다. 결국 자기 자신과 비교하니까 항상 같은 값이고 `>=`가 참이 된다.

## Q43. 밝기 계산하면서 total도 같이 더해도 되나?
가능했다. 그러면 나중에 합을 구하려고 brightness_map을 한 번 더 돌지 않아도 된다. 다만 처음 배우는 입장에서는 계산 단계를 나눠 놓은 코드가 이해하기 더 쉬웠다.

## Q44. sum()을 쓰면 짧아지나?
짧아진다. 하지만 2차원 리스트라 `sum(sum(row) for row in ...)`처럼 써야 해서 처음에는 오히려 for문이 더 알아보기 쉬웠다.

## Q45. `join()`도 쓸 수 있나?
가능했다. 0과 1을 리스트에 담고 `''.join(...)` 하면 한 줄 문자열이 된다. 지금 코드는 `+=` 방식이 더 단순해서 그대로 써도 된다고 생각했다.

## Q46. 변수 이름을 길게 쓰는 이유는?
처음에는 `a`, `b`가 편해 보였는데 나중에 다시 보면 뭔지 모른다. `threshold`, `brightness_map`처럼 쓰면 이름만 봐도 대충 역할이 보였다.

## Q47. 주석은 많이 쓰면 좋은 건가?
모든 줄을 한국어로 번역해서 적는 것보다는 왜 `//`를 쓰는지, 왜 `>=`인지처럼 헷갈릴 부분에 적는 게 낫다는 걸 알았다.

## Q48. 함수는 잘게 쪼갤수록 무조건 좋은가?
그건 아니었다. 너무 많이 나누면 오히려 따라가기 힘들다. 밝기 계산, 입력 변환, threshold, 흑백 변환 정도처럼 역할이 확실히 다른 부분을 나누는 게 중요했다.

## Q49. 테스트는 왜 여러 개 해야 하지?
예제 하나만 맞으면 다른 경우에서 틀릴 수 있기 때문이다. 그래서 검정, 흰색, 1x1, 섞인 값 같은 걸 따로 해봤다.

## Q50. 예상값하고 실제값을 왜 따로 적지?
먼저 내가 답을 예상하고 나서 코드를 돌려야 검증이 된다. 실행하고 나온 값에 맞춰 예상값을 적으면 테스트 의미가 없었다.

## Q51. 시간 복잡도 O(HW)는 무슨 뜻이지?
아직 Big-O가 익숙하지는 않지만 결국 픽셀이 H×W개이고 그 픽셀들을 몇 번 반복해서 보는 구조라서 이미지 크기에 비례해 시간이 늘어난다는 뜻으로 이해했다.

## Q52. 공간 복잡도는?
brightness_map에 픽셀 수만큼 값을 저장하니까 이미지가 커지면 저장할 값도 같이 많아진다. 그래서 이것도 대략 H×W에 비례한다고 이해했다.

## Q53. 최대 100x100이면 큰 건가?
최대 10,000픽셀이다. 지금 같은 단순 반복문으로 처리하기에는 큰 숫자가 아니라서 굳이 복잡한 최적화까지 할 필요는 없었다.

## Q54. 한 줄에 픽셀이 W개가 맞는지 확인하려면?
`len(pixels) == width`로 확인할 수 있었다. 과제에서는 정상 입력이 온다고 가정하지만 디버깅할 때는 유용해 보였다.

## Q55. RGB가 세 조각인지도 확인 가능?
`len(pixel.split(','))`가 3인지 보면 된다. `255,0`처럼 값이 부족하면 나중에 r,g,b로 나눠 담을 때 오류가 난다.

## Q56. RGB가 300 같은 값이면?
정수 변환은 되지만 정상 RGB 범위는 아니다. 실제 프로그램이라면 0~255 범위를 검사할 수도 있다는 걸 알았다.

## Q57. 실제 이미지 밝기 공식은 다르다던데?
실제로는 색마다 가중치를 다르게 주기도 한다고 했다. 하지만 이 과제에서는 문제에서 `(R+G+B)//3`이라고 정했으니 그걸 그대로 써야 한다.

## Q58. 평균 말고 중앙값을 쓰면 안 되나?
다른 문제라면 가능할 수 있지만 여기서는 평균으로 하라고 정해져 있어서 바꾸면 안 된다. 알고리즘 문제에서는 문제 조건을 그대로 따르는 게 먼저였다.

## Q59. threshold가 0도 될 수 있나?
전체가 검정이면 된다. 그리고 이 경우 결과가 전부 1이 된다는 게 처음에는 가장 헷갈렸다.

## Q60. threshold가 255도 되나?
전체가 흰색이면 된다. 255와 255를 비교해도 `>=`라서 결과는 1이다.

## Q61. brightness가 threshold와 딱 같으면?
1이다. '이상'이라는 단어를 코드로 바꾸면 `>=`라는 걸 다시 확인했다.

## Q62. 결과 한 줄 길이는 왜 W와 같지?
픽셀 하나가 결과 문자 하나로 바뀌기 때문이다. W가 3이면 결과도 `110`처럼 세 글자여야 한다.

## Q63. 결과 줄 수는 왜 H와 같지?
입력 한 행마다 결과 한 행을 만들기 때문이다. 행 수가 다르면 반복문이나 append 위치를 잘못 둔 걸 의심할 수 있다.

## Q64. `1 1 0`처럼 공백 넣으면 안 되나?
안 된다. 문제에서는 `110`을 요구한다. 자동 채점은 사람처럼 '비슷하네' 하고 넘어가지 않는다는 점을 기억했다.

## Q65. 노트북에서는 왜 input을 안 쓰지?
모든 셀을 실행할 때마다 입력을 기다리면 불편해서 학습용 코드는 예제 값을 변수에 직접 넣었다. 최종 `problem02.py`에서는 input을 사용한다.

## Q66. 노트북 셀을 하나씩 따로 실행 가능하게 한 이유는?
중간 셀부터 눌러도 앞 셀에서 만든 변수가 없다고 오류가 나지 않게 하려고 각 셀에 필요한 값을 다시 넣었다.

## Q67. VS Code에서 ipynb 실행이 안 되면?
코드만 볼 게 아니라 Jupyter 커널이나 Python 인터프리터가 선택됐는지도 확인해야 한다는 걸 알았다.

## Q68. 가상환경은 왜 쓰지?
프로젝트별 Python 환경을 따로 관리하려고 쓴다. 이번 문제는 외부 패키지가 거의 필요 없어서 큰 차이는 없지만 VS Code에서 어떤 Python을 쓰는지 구분하는 데 도움이 됐다.

## Q69. 파일이 있는데 `can't open file`이 왜 뜨지?
터미널 현재 위치가 파일 위치와 다르면 생길 수 있었다. 코드 오류가 아니라 경로 문제였다.

## Q70. 오류 메시지가 너무 길면 어디부터 보지?
마지막 줄에 `ValueError`, `NameError` 같은 오류 이름과 이유가 나오는 경우가 많아서 거기부터 보고 위쪽의 파일/줄 번호를 확인하면 됐다.

## Q71. AI한테 오류 질문할 때 뭘 같이 보내야 하지?
'안 돼'만 쓰는 것보다 오류 메시지, 코드, 입력값, 원래 기대한 결과를 같이 보여주는 게 훨씬 정확했다.

## Q72. AI 답이 맞는지 어떻게 확인하지?
작은 숫자를 직접 손으로 계산하고 코드 결과와 비교하는 게 제일 확실했다. 특히 예제와 경계값을 같이 확인했다.

## Q73. assert는 뭐에 쓰지?
예상한 값하고 실제 함수 결과가 같은지 자동으로 확인할 수 있었다. 테스트할 때는 편하지만 최종 사용자 출력에 쓰는 건 아니라는 것도 알았다.

## Q74. 파일이 여러 개인 이유는?
`problem02.py`는 최종 실행 코드, `problem02_learning.ipynb`는 공부하면서 확인한 코드, `AI_LOG.md`는 질문 과정, `SOLUTION_GUIDE.md`는 최종 설명으로 역할을 나눴다.

## Q75. 다시 처음부터 풀면 어떻게 할까?
한 번에 전체 코드를 치려고 하지 않고 `RGB 하나 분리 → 밝기 하나 계산 → 한 행 처리 → 전체 밝기 맵 → 평균 → 0/1 변환 → 테스트` 순서로 조금씩 만들면 될 것 같다.

---

## 마지막 정리

처음에는 `255,0,0` 같은 입력부터 왜 저렇게 생겼는지 몰랐고 `//`, `map`, 2차원 리스트도 한꺼번에 나오니까 복잡해 보였다. 하나씩 출력해 보면서 문자열이 어떻게 나뉘고 숫자로 바뀌는지 확인하니 뒤쪽 계산도 따라갈 수 있었다. 아직 Python 문법을 전부 외운 건 아니지만, 이 문제는 입력을 잘 나누고 밝기 평균을 구한 다음 기준과 비교하는 순서라고 설명할 수 있게 됐다.
